<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <p><%= f.text_field :title %>

  <h4>Body</h4>
  <p><%= f.text_area :body %></p>
  <%= f.submit '保存' %>

  <p><%= link_to 'Show', show_book_path(@book.id) %> | <%= link_to 'Back', '/books' %></p>
<% end %>